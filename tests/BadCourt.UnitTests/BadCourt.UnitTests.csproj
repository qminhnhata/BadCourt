<Project Sdk="Microsoft.NET.Sdk">

  <!-- xunit.v3 test projects are executables: the test app hosts Microsoft.Testing.Platform
       itself rather than being loaded by a VSTest runner. -->
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <IsTestProject>true</IsTestProject>
  </PropertyGroup>

  <ItemGroup>
    <!-- The dispatcher is tested through a real container, not a hand-assembled one. -->
    <PackageReference Include="Microsoft.Extensions.DependencyInjection" />
    <PackageReference Include="Shouldly" />
    <PackageReference Include="xunit.v3" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\src\api\BadCourt.SharedKernel\BadCourt.SharedKernel.csproj" />
  </ItemGroup>

</Project>
